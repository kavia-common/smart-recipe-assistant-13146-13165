import httpClient from '../../../services/httpClient';

/**
 * Auth API service placeholder.
 * Replace with real endpoints and functions later.
 */
const authApi = {
  // Example: signIn: (payload) => httpClient.post('/auth/signin', payload),
};

export default authApi;
