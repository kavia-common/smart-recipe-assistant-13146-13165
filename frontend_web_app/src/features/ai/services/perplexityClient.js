import httpClient from '../../../services/httpClient';

/**
 * AI client placeholder for recipe generation/suggestions.
 * Replace with real AI endpoints later.
 */
const perplexityClient = {
  // Example: suggest: (ingredients) => httpClient.post('/ai/suggest', { ingredients }),
};

export default perplexityClient;
