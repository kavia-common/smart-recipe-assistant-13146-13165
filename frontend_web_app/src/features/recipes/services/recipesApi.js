import httpClient from '../../../services/httpClient';

/**
 * Recipes API service placeholder.
 * Replace with real endpoints and functions later.
 */
const recipesApi = {
  // Example: list: () => httpClient.get('/recipes'),
};

export default recipesApi;
