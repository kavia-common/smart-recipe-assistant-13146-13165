# Frontend environment variables (copy to .env and adjust values as needed)

# Base URL for backend API (e.g., http://localhost:4000)
REACT_APP_API_BASE_URL=http://localhost:4000

# Default HTTP timeout in milliseconds
REACT_APP_API_TIMEOUT_MS=15000
